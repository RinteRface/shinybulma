% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bulma-footer.R
\name{bulmaFooter}
\alias{bulmaFooter}
\title{Add a footer}
\usage{
bulmaFooter(..., container = TRUE, centered = TRUE)
}
\arguments{
\item{...}{any element.}

\item{container}{set to \code{TRUE} to wrap in \code{\link{bulmaContainer}}.}

\item{centered}{wheter to center the text.}
}
\description{
Add a footer
}
\examples{
if(interactive()){
library(shiny)

ui <- bulmaPage(
  bulmaHero(
    fullheight = TRUE,
    color = "primary",
    bulmaHeroHead(
      bulmaContainer(
        bulmaTitle("Hero Title")
      )
    ),
    bulmaHeroHead(
      bulmaContainer(
        bulmaSubtitle(
          "Some content"
        )
      )
    ),
    bulmaHeroFoot(
      bulmaContainer(
        "The footer"
      )
    )
  )
)

server <- function(input, output){}

shinyApp(ui, server)
}
}
